# 自前 F-Droid リポジトリの設定。
#
# 署名鍵 (fdroid.keystore) はここには置かない。CI が実行時に配置し、
# パスワードは環境変数から読ませる。
---
repo_url: https://pad01g.github.io/wordquiz-fdroid/fdroid/repo
repo_name: pad01g's apps
# **ここにアプリ名を並べないこと。** 並べるとアプリを足すたびに書き足しが要り、
# 書き足し忘れた瞬間からこの文が嘘になる。各アプリの説明は
# metadata/<applicationId>.yml が持っていて、利用者にはそちらが出る。
repo_description: |-
  pad01g が個人で作ったアプリを配布するための自前リポジトリです。
  一度追加しておけば、あとから増えたアプリも自動で一覧に出ます。

  どれも広告と利用者追跡を入れていません。
  外部のサーバを使うものには、その旨をアプリごとに明記しています。
repo_icon: icon.png

# 古いバージョンも repo に残す (アーカイブへ移さない)。
# 練習用でバージョン数が少ないため、分割しても手間が増えるだけ。
archive_older: 0

sdk_path: $ANDROID_HOME
keystore: fdroid.keystore
repo_keyalias: wordquizrepo
keystorepass: {env: FDROID_KEYSTORE_PASS}
keypass: {env: FDROID_KEY_PASS}
